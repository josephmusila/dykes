import 'package:bikes/models/userModel.dart';
import "package:flutter/material.dart";

class RepairsScreen extends StatelessWidget {
 UserDataModel user;

 RepairsScreen({required this.user});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}